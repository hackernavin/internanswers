function data(){ 

	str ="hi dude";
	var chars = [(str.split('')];
  	for (var i = 0; i < chars.length; i++) {
    	var value = chars[i].charCodeAt(0);
    	var result= [String.CharfromCode(value)]; 	
 	 }
   	     for (var j=0;j<result.length;j++){				
	     return result[j].join('');

		}

}